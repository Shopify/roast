This workflow demonstrates the tool configuration feature.

Try using the command tool to:
1. List files with 'ls'
2. Show current directory with 'pwd'
3. Echo a message with 'echo "Hello from configured commands!"'

Note that commands not in the allowed list (like 'rm', 'git', etc.) will be rejected.
