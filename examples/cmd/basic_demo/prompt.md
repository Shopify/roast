# Basic Command Demonstration

Let me show you how command functions work:

**Step 1: Where are we?**
Using `pwd()` to show our current working directory:

**Step 2: What's here?**
Using `ls(args: "-la")` to list all files with details:

**Step 3: Display a message**
Using `echo(args: "🎉 Command functions are working!")`:

**Step 4: Show a specific directory**
Using `ls(args: "examples/")` to see what examples are available:

Each command I use is explicitly allowed in your workflow configuration. This keeps your system secure while giving me the tools I need to help you!
